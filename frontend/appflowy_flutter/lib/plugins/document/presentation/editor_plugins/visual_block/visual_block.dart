/// The shared frame every visual block wears.
///
/// A diagram, a mind map, an equation and a drawing differ only in what they
/// contain: the card, the quiet header, the hover controls, the three-dot
/// menu, the fullscreen route and the export dialog are one implementation,
/// so a fifth kind of block is a body and a `VisualBlockActions`.
library;

export 'visual_block_actions.dart';
export 'visual_block_frame.dart';
export 'visual_block_fullscreen.dart';
export 'visual_block_style.dart';
